{% layout 'theme' title: "archival manifesto"
  , area: "manifesto" %}

<div class="manifesto">
  <h1 class="hero-message mb-16">{{ objects.manifesto.title }}</h1>
  <div class="manifesto-body md w-full max-w-screen-md px-10 text-lg font-sans">
    {{ objects.manifesto.content }}
  </div>
</div>
