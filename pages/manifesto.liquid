{% layout 'theme' title: "archival manifesto"
  , area: "home" %}

<div class="mb-10 md:mt-20 py-20 flex flex-col items-center gap-10 bg-pink-light shadow-md">
  <h1 class="font-header text-2xl">{{ objects.manifesto.title }}</h1>
  <div class="md w-full max-w-screen-md px-10 text-lg font-sans">
    {{ objects.manifesto.content }}
  </div>
</div>
